"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const base_class_1 = tslib_1.__importStar(require("../../lib/base-class"));
class Remove extends base_class_1.default {
    async run() {
        const { flags: { worksheetTitle = '', spreadsheetId }, } = this.parse(Remove);
        this.start('Removing worksheet');
        await this.gsheet.removeWorksheet(worksheetTitle, spreadsheetId);
        this.stop();
        this.logRaw(`Worksheet "${worksheetTitle}" successfully removed`, { operation: this.id, worksheetTitle });
    }
}
exports.default = Remove;
Remove.description = 'Remove a worksheet with the specified title from the spreadsheet';
Remove.examples = [
    `$ gsheet worksheet:remove --spreadsheetId=<spreadsheetId> --worksheetTitle=<worksheetTitle>

Worksheet "<worksheetTitle>" successfully removed
`,
];
Remove.flags = Object.assign(Object.assign({}, base_class_1.default.flags), { worksheetTitle: base_class_1.worksheetTitle,
    spreadsheetId: base_class_1.spreadsheetId });
